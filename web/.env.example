# シナリオ配信のベース URL（GCS バケットの公開 URL）。
# 未設定の場合はローカルの public/scenarios/（pnpm --filter web sync:scenarios で生成）を参照する。
VITE_SCENARIO_BASE_URL=https://storage.googleapis.com/your-scenario-bucket

# Google Analytics 計測 ID。未設定なら計測なしで動作する。
VITE_GA_MEASUREMENT_ID=

/**
 * SKELETON — do not import directly.
 * Copy to: domain/value-objects/（DECISION_TREE Q5 — 値オブジェクト）
 *
 * TODO: 正規化（trim / case）と等価性の定義（値ベース）。
 *
 * 既知の注意:
 * - VO に副作用のある I/O を持たせない。
 * - 金額・通貨ペアなどは専用 VO にまとめ、プリミティブ握りつぶしを避ける。
 */

export class ExampleValueObject {
  // TODO: readonly + factory with validation
}

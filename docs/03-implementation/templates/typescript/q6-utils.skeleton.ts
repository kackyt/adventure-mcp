/**
 * SKELETON — do not import directly.
 * Copy to: shared/utils/（DECISION_TREE Q6 — ユーティリティ）
 *
 * TODO: 本当に横断か？ドメイン寄りなら domain 側の helper を検討。
 *
 * 既知の注意:
 * - 「便利関数」の肥大化を防ぎ、責務ごとにファイル分割する。
 * - 外部依存のある util は infrastructure に近づけることを検討。
 */

export function exampleUtil(_value: string): string {
  // TODO: pure, deterministic helper
  return _value;
}

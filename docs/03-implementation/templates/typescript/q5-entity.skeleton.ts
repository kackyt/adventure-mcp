/**
 * SKELETON — do not import directly.
 * Copy to: domain/entities/（DECISION_TREE Q5 — エンティティ）
 *
 * TODO: 識別子・不変条件・ファクトリ（parse）をドメイン言語で表現。
 *
 * 既知の注意:
 * - インフラ型（DB の行型）をエンティティに持ち込まない。
 * - 集約ルートと子エンティティの責務境界を DOMAIN.md と整合させる。
 */

export class ExampleEntity {
  // TODO: private fields + invariants
}

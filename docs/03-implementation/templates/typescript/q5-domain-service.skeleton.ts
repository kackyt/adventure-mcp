/**
 * SKELETON — do not import directly.
 * Copy to: domain/services/（DECISION_TREE Q5 — ドメインサービス）
 *
 * TODO: どのエンティティにも自然に属さないルールのみを置く（濫用に注意）。
 *
 * 既知の注意:
 * - ドメインサービスにリポジトリを詰め込まない（ユースケースの責務）。
 * - ステートレスを基本とし、必要なら明示的な入力引数に閉じる。
 */

export function exampleDomainRule(_input: unknown): boolean {
  // TODO: pure domain logic
  return false;
}
